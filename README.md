# Start

- `yarn` to install
- `yarn build` to build for production
- `yarn start` to start for production
- `yarn be:dev` to start backend development (serves fe if built)
- `yarn fe:dev` to start frontend development
